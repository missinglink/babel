{
  "name": "babel",
  "private": false,
  "version": "0.0.1",
  "description": "Babel is a protocol independent SOA service adapter for node.js (still in development)",
  "contributors": [],
  "dependencies": {
    "should": "~1.2.2",
    "eventemitter2": "~0.4.11"
  },
  "scripts": {
    "test": "export NODE_PATH=`pwd` && node node_modules/mocha/bin/mocha --recursive --reporter spec --compilers coffee:coffee-script test"
  },
  "devDependencies": {
    "mocha": "*",
    "coffee-script": "~1.6.1"
  },
  "readme": "# Babel is a protocol independent SOA service adapter for node.js (still in development)",
  "engines": {
    "node": "0.10.x",
    "npm": "1.1.x"
  }
}
